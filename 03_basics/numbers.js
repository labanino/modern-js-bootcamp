/*
// Create a number and assign to a variable
let age = 26

// Add
let addResult = age + 1   // 27

// Subtract
let subtractResult = age - 26.5

// Multiply
// let x = 11
// let num = (x + 1) * 2
let multiplyResult = age * 10

// Divide
// -0.5 // 260
let divideResult = age / 2   // 13

// Parenthesis can be used to make sure one operation happens before another
let result = (age + 3) * 7 //203
*/

let studentScore = 41 // correct questions
let maxScore = 100
let percent = (studentScore / maxScore) * 100 // (value / total value) × 100%

console.log(percent)